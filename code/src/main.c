#include "../include/manual.h"
#include "../include/menu.h"

int main(int argc, char* charv[]) {
    begin();
    if (!game_mode-1)
    {
        if(game_mode == 2) {
            return 0;
        }
        handleManualGame();
    }
    return 0;
}